import { FormControl } from './form-control';

describe('FormControl', () => {
  it('should create an instance', () => {
    const directive = new FormControl();
    expect(directive).toBeTruthy();
  });
});
